#!/usr/bin/env bash
set -euo pipefail
# Image prep for image-to-video models. Usage: ./tools/prep <subcommand> [args]
# See tools/README.md
source "$(cd "$(dirname "$0")" && pwd)/_env.sh"
exec "$PYTHON" "$ROOT/tools/prep.py" "$@"
